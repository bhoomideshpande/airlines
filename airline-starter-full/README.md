# Airline App - Full Starter

Minimal fullstack starter: React (Vite) frontend + Node/Express backend (monorepo).

## Quick start

### Backend
- `cd backend`
- copy `.env.example` to `.env` and fill values
- `npm install`
- `npm run dev`

### Frontend
- `cd frontend`
- `npm install`
- `npm run dev`
